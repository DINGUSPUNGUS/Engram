/**
 * @engram/ui — shared components move here from apps/web/src/components/ui
 * the moment a second surface (e.g. the VSCode extension webview) needs them.
 * Keeping the package wired now means that move is a cut-and-paste, not a
 * build-system project.
 */
export {};
