"""engram-mcp: the MCP server that lets assistants (Claude, Cursor, …) recall and
store memories. Deliberately empty until roadmap phase 7 — see apps/mcp/README.md
for the planned tool surface."""

__version__ = "0.1.0"
