"""In-tree reference plugin(s), proving the architecture (ADR-0024 §"Reference
plugin"). Nothing here is imported by ``engram_plugins`` itself — a composition
root opts in explicitly, exactly like the reference README of any other
adapter package."""
